using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExitNode : MonoBehaviour
{
    /*public float minReward = 0.05f;
    public float maxReward = .5f;

    public float radius = 10f;

    public SimulatorAgent _agent;

    private void Update()
    {
        Collider[] hitColliders = Physics.OverlapSphere(transform.position, radius, 1<<LayerMask.NameToLayer("Escapee"));
        int i = 0;
        float rewardRes = 0;
        while (i < hitColliders.Length) {
            rewardRes += Mathf.Lerp(minReward, maxReward, Vector3.Distance(transform.position, hitColliders[i].transform.position) / radius);
        }

        _agent.AddReward(rewardRes);
        */
}
